The program runs with the command 'java -jar PA4.jar'.

In the program:
1) Two random 2D arrays are created which represent the walls.
2) The walls are passed to the Maze class which concatenates the walls.
3) A graph with adjacency list is created using the maze.
4) DFS and BFS is performed with the given graph.
